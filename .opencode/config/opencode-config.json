{
  "$schema": "https://quickagents.ai/schemas/opencode-config.json",
  "version": "1.0.0",
  "description": "OpenCode 配置覆盖 - 调整检测阈值",
  
  "doomLoop": {
    "enabled": true,
    "stuckThreshold": 5,
    "oscillationThreshold": 3,
    "repeatThreshold": 3,
    "windowSize": 20,
    "excludeTools": ["git", "npm", "pip", "curl", "wget", "ssh", "scp"],
    "excludePatterns": [
      "git push",
      "git pull",
      "git fetch",
      "npm install",
      "pip install"
    ]
  },
  
  "loopDetection": {
    "enabled": true,
    "threshold": 3,
    "windowSize": 20,
    "excludeTools": ["git", "npm", "pip"]
  },
  
 D:\Projects\QuickAgents\.opencode\config\opencode-config.json
    "default": 120000,
    "network": 60000,
    "file": 30000
  }
}
